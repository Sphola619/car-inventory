// Configuration object for MongoDB connection
const dbConfig = {
  // Replace the URL below with your own MongoDB connection string
  url: 'your-mongodb-connection-string-here'
};

// Export the configuration object to be used in other parts of the application
export default dbConfig;